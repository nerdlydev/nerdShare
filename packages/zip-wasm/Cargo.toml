[package]
name = "zip-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2.111"
js-sys = "0.3.88"
zip = { version = "8.1.0", default-features = false, features = ["deflate"] }

[profile.release]
opt-level = "z"
lto = true
